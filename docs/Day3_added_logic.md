### Очередь URL — CrawlerQueue
**Файл**: src/crawler/queue.py
Что добавлено:
- Асинхронная очередь с приоритетами для URL (asyncio.PriorityQueue)
- Контроль дубликатов через self._seen
- Методы:
  - add_url(url: str, priority: int = 0) — добавить URL в очередь с приоритетом 
  - get_next() -> (url, priority) | None — получить следующий URL из очереди 
  - mark_processed(url: str) — пометить URL как успешно обработанный 
  - mark_failed(url: str, error: str) — пометить URL как неудачный 
  - get_stats() -> dict — статистика очереди (в очереди, обработано, ошибки, уникальные URL)

**Зачем**: управлять потоком URL для асинхронного краулинга, обеспечивать уникальность и приоритет обработки.

### Семафоры и ограничение конкурентности — SemaphoreManager
**Файл**: src/crawler/semaphore_manager.py

Что добавлено:
- Глобальное ограничение количества одновременных запросов (global_limit)
- Ограничение на количество одновременных запросов к одному домену (per_domain_limit)
- Асинхронный менеджер контекста для использования в async with 
- Методы для отслеживания активных задач

**Зачем**: предотвратить перегрузку сайтов, контролировать скорость запросов и соблюдать ограничения по конкурентности.

### Управление состоянием URL
**Файл**: AsyncCrawler
- visited_urls: set[str] — множество посещённых URL 
- failed_urls: dict[str, str] — URL → ошибка загрузки 
- processed_urls: dict[str, dict] — URL → результат парсинга

**Зачем**: избежать повторной загрузки, собирать статистику ошибок и успешно обработанных страниц.

### Фильтрация URL
**Файл**: AsyncCrawler
- allowed_domains: list[str] — разрешённые домены 
- include_patterns: list[str] — паттерны для обязательного включения URL 
- exclude_patterns: list[str] — паттерны для исключения URL

Методы:
- _is_allowed_domain(url) — проверка домена
- _is_allowed_url(url) — проверка по include/exclude паттернам

- **Зачем**: избежать повторной загрузки, собирать статистику ошибок и успешно обработанных страниц.

### Основной краулинг — AsyncCrawler.crawl()
**Что добавлено:**: 
- Использование CrawlerQueue для управления URL
- Автоматическое добавление найденных ссылок из страниц в очередь
- Ограничение максимальной глубины обхода (max_depth)
- Ограничение максимального числа страниц (max_pages)
- Асинхронные воркеры для параллельной обработки страниц
- Логирование прогресса в реальном времени:
  - 📄 Количество обработанных страниц
  - ⏳ Количество в очереди
  - ❌ Количество ошибок
  - ⚡️ Скорость обработки (страниц/сек)

**Зачем**: краулер стал полностью асинхронным, управляемым и безопасным, без повторной загрузки страниц.

### Интеграция с main.py
**Что изменилось:**: 
- Добавлена очередь CrawlerQueue
- Запуск воркеров для параллельного краулинга
- Сбор статистики и сохранение JSON
- Отображение прогресса и статистики очереди

### Тесты
**Файл**: tests/test_crawler.py
**Что добавлено/обновлено:**: 
- Тестирование очереди с приоритетами
- Тест ограничения глубины (max_depth)
- Тест фильтрации URL (allowed_domains, include_patterns, exclude_patterns)
- Проверка уникальности URL в visited_urls
- Тесты работы метода crawl() вместо старых fetch_urls и fetch_and_parse

### Прогресс и метрики

- _progress_logger() выводит в лог:
  - Обработано страниц
  - В очереди
  - Ошибки
  - Скорость обработки
- Обновляется асинхронно каждые N секунд

**Зачем**: удобно контролировать работу краулера в реальном времени.