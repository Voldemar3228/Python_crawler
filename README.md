
# Python Async/SYNC Web Crawler

**Проект:** Асинхронный и синхронный веб-краулер для масштабного сбора данных с сайтов.  
Поддерживает прогресс-бар, статистику, сохранение результатов и конфигурацию через YAML/CLI.

---

## 🔹 Особенности

- Асинхронный и синхронный режимы
- Поддержка **максимальной глубины** (max_depth)
- Ограничение по **числу запросов в секунду**
- Фильтры URL по домену и шаблонам (include/exclude)
- Автоматический **retry и обработка ошибок**
- Сбор статистики и экспорт:
  - JSON
  - HTML отчёт
- Поддержка хранения данных:
  - JSON
  - SQLite

---

## 🔹 Установка

```bash
git clone <репозиторий>
cd <проект>
python -m venv venv
source venv/bin/activate  # Linux / Mac
venv\Scripts\activate     # Windows
pip install -r requirements.txt
````

---

## 🔹 Примеры использования

### 1. Синхронный краулер

```python
from benchmark.sync_crawler import crawl_sync

start_urls = ["https://www.wikipedia.org/"]
results, count = crawl_sync(start_urls, max_pages=10)
print(f"Обработано страниц: {count}")
for r in results:
    print(r)
```

### 2. Асинхронный краулер

```python
import asyncio
from benchmark.async_crawler import crawl_async

async def main():
    results, count = await crawl_async(max_pages=10)
    print(f"Обработано страниц: {count}")
    for r in results:
        print(r)

asyncio.run(main())
```

### 3. AdvancedCrawler (с конфигурацией и логированием)

```python
from advanced_crawler import AdvancedCrawler

crawler = AdvancedCrawler(config_path="config.yaml")
asyncio.run(crawler.run())
```

---

## 🔹 API

### `crawl_sync(start_urls, max_pages=100)`

* **start_urls** — список стартовых URL
* **max_pages** — максимальное количество страниц
* **Возвращает:** `(results: list[str], visited_count: int)`

---

### `crawl_async(max_pages)`

* **max_pages** — максимальное количество страниц
* **Возвращает:** `(results: list[dict], count: int)`
* Асинхронная версия, поддерживает прогресс-лог и статистику.

---

### `AdvancedCrawler`

#### Конструктор:

```python
AdvancedCrawler(config_path=None, cli_args=None)
```

* **config_path** — путь к YAML конфигу
* **cli_args** — словарь с переопределением настроек

#### Методы:

* `run()` — запускает краулер, возвращает список обработанных страниц
* Логирует прогресс, ошибки, сохраняет статистику (JSON + HTML)

---

## 🔹 Конфигурация (YAML)

Пример `config.yaml`:

```yaml
start_urls:
  - "https://www.wikipedia.org/"
max_pages: 100
crawler:
  max_concurrent: 10
  max_depth: 2
  rate_limit: 5
  respect_robots: false
  include_patterns: []
  exclude_patterns: []
  allowed_domains: ["wikipedia.org"]
storage:
  type: "json"
  path: "results.json"
log_file: "crawler.log"
log_level: "INFO"
```

* `max_concurrent` — число одновременных запросов
* `max_depth` — глубина обхода ссылок
* `rate_limit` — запросов в секунду
* `respect_robots` — учитывать robots.txt
* `storage` — куда сохранять результаты

---

## 🔹 Бенчмарк

```bash
python src/benchmark/run_benchmark.py
```

* Сравнивает **SYNC vs ASYNC** по времени и потреблению памяти
* Можно менять нагрузку через `pages_list = [100, 500, 1000]`

---

## 🔹 Логи и статистика

* Прогресс логируется в консоль: количество обработанных страниц, скорость, ошибки
* Статистика экспортируется:

  * `stats.json` — общее количество, успешные/неуспешные страницы, статус-коды
  * `report.html` — визуальный отчёт с графиками

---

## 🔹 Требования

* Python 3.11+
* aiohttp
* beautifulsoup4
* tqdm
* pyyaml (для конфигурации)
* sqlite3 (для SQLite-хранилища)

---

## 🔹 Автор

Владимир Комиссаров